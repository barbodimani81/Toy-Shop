from django.urls import path, include
from rest_framework import routers

# from .views import SignUp

# Create a router for the user resource
# user_router = routers.DefaultRouter()
# user_router.register('', SignUp, basename='user-create')

# Include the user URLs in the urlpatterns
# urlpatterns = [
#     path('users/', SignUp)  # include(user_router.urls)),
# ]
